# ─────────────────────────────────────────────────────────────
#  requirements.txt — Colorectal Cancer Detection Pipeline
#  Install with:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# Core deep learning
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Timm — Swin, ViT, ConvNeXt, BEiT, DeiT, Xception, etc.
timm>=0.9.12

# Graph neural networks
torch-geometric>=2.4.0
# Install torch-geometric extras separately:
#   pip install torch_scatter torch_sparse torch_cluster
#   (match your torch version — see https://data.pyg.org/whl/)

# Image processing
opencv-python>=4.8.0
Pillow>=10.0.0
scikit-image>=0.21.0

# Scientific computing
numpy>=1.24.0
scipy>=1.11.0

# Machine learning metrics
scikit-learn>=1.3.0

# Visualisation
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.65.0
pandas>=2.0.0

# Optional: mixed precision / accelerated training
# accelerate>=0.24.0

# ─────────────────────────────────────────────────────────────
#  Added for the deep ablation study (evaluation/*)
# ─────────────────────────────────────────────────────────────
# FLOPs counting in evaluation/compute_cost.py (optional — the script
# degrades gracefully to "None" for FLOPs if neither is installed)
thop>=0.1.1.post2209072238
# fvcore>=0.1.5.post20221221

# Peak CPU RAM measurement in compute_cost.py when no GPU is present
psutil>=5.9.0


